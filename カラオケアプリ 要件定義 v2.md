# どこでもカラオケセット 要件定義書 v2

> Web版プロトタイプ（KARAOKE STUDIO / karaoke.html）を土台に、macOS デスクトップアプリとして再設計した版。
> v1 からの主な変更点：**2ファイル方式による解析精度の抜本的改善**、**文字単位タイミング**、**Undo/Redo とショートカット体系**、**曲ごとの音ズレオフセット**、**プロジェクトファイル形式の確定**。

---

## 1. 概要

### 1.1 アプリ名

**どこでもカラオケセット**（英字表記: `Dokokara Karaoke Set`）

プロジェクトファイル拡張子: **`.dokokara`**

### 1.2 目的

手持ちの楽曲（オンボーカル音源＋オフボーカル音源）から歌のメロディを自動解析し、ユーザーが用意した歌詞とタイミングを結びつけて、自分専用のカラオケコンテンツを作成・再生できるデスクトップアプリケーションを開発する。

### 1.3 背景

ブラウザで動作する HTML プロトタイプで以下を実証済み。

- Web Audio API による音声ファイルからのピッチ（基本周波数）自動検出
- 検出結果と手動入力歌詞を組み合わせたタイミング自動割り当て
- ドラッグ操作による歌詞タイミングの手動補正エディタ
- 歌詞テキストの文字送り（カラオケ字幕演出）を伴う再生モード

プロトタイプで判明した課題は以下。本要件定義書はこれらの解決を主眼とする。

| # | 課題 | v2 での対応 |
|---|------|-------------|
| P-1 | フルミックス音源への自己相関がベース／キックを拾い、歌のメロディを検出できない | 2ファイル方式＋差分ボーカル抽出（§4.4） |
| P-2 | 再生時に原曲のボーカルが鳴りっぱなしで、カラオケとして成立しない | オフボーカル音源を再生系統に分離（§4.3） |
| P-3 | 文字送りが行全体の線形補間で、実際の発音と露骨にズレる | 文字単位タイミング（§4.6） |
| P-4 | 誤ドラッグを取り消す手段がない | Undo/Redo（§4.8） |
| P-5 | キーボード操作が一切なく、編集速度が出ない | ショートカット体系＋タップ入力（§4.9） |
| P-6 | ドラッグが完全フリーで、狙った位置に合わせにくい | スナップ機構（§4.7.3） |
| P-7 | 出力レイテンシ分だけ字幕が音より先行する | 曲ごとの音ズレオフセット（§4.11） |
| P-8 | 作ったものが保存できず、毎回やり直しになる | プロジェクト管理（§4.1） |
| P-9 | フォントを外部 CDN から取得しており、オフラインで崩れる | フォント同梱（§6.3） |

### 1.4 想定ユーザー・利用規模

- 開発者本人を含む **2名**。バンドの練習用カラオケを内製する用途。
- 一般配布・ストア公開は行わない。ビルド成果物を直接手渡し（AirDrop / USB / ファイル共有）する。

### 1.5 スコープ外

明確に対象外とする。

- 音声認識（Speech-to-Text）による歌詞の自動書き起こし ※歌詞は必ずユーザーが手入力・貼り付けする
- ピッチカーブ自体を波形単位で描き直す本格的なボーカルエディタ（Melodyne 等相当）
- 歌唱の採点機能（音程一致度・タイミング精度のスコアリング）
- 楽曲配信・共有・クラウド同期機能。本アプリはローカルでの個人利用を前提とする
- 機械学習によるボーカル音源分離（Demucs / Spleeter 等）。※オフボーカル音源はユーザーが用意する
- Windows / Linux 対応
- Intel Mac（x64）対応
- 自動アップデート機構

---

## 2. 用語集

| 用語 | 説明 |
|------|------|
| ピッチ（音程） | 音の高さ。基本周波数（Hz）で表現される |
| オンボーカル音源 | 歌が入っている通常の楽曲ファイル。**解析専用**に使う |
| オフボーカル音源 | 歌が抜かれた伴奏のみのファイル（カラオケ音源）。**再生専用**に使う |
| 差分ボーカル | オンボーカルからオフボーカルを減算して得た、歌声のみに近い信号 |
| フレーズ区間 | 有声（発声）が検出された連続区間。歌詞1行の割り当て先候補 |
| ライン（行） | 歌詞1行。プロジェクト上の最小の編集ブロック単位 |
| トークン | 行を構成する文字送りの単位。日本語は原則1モーラ、英単語は1単語 |
| ルビ | 漢字トークンに付与する読み仮名 |
| プレイヘッド | 現在の再生位置を示すタイムライン上のマーカー |
| オンセット | 音の立ち上がり。文字境界の候補として用いる |
| オフセット | 音声出力遅延を補正するための、表示側の時間シフト量 |

---

## 3. 画面構成・遷移

```
        ┌──────────────────────────────────────────┐
        │                                          │
   [0. ホーム] ──新規──> [1. 準備] ──> [2. 解析中] ──> [3. 編集] <──> [4. 本番]
        │                                              │
        └────────既存プロジェクトを開く─────────────────┘

   [設定] … いずれの画面からも ⌘, で開けるモーダル
```

| # | 画面名 | 役割 |
|---|--------|------|
| 0 | ホーム（Home） | プロジェクト一覧・新規作成・開く・削除・複製 |
| 1 | 準備（Setup） | 音源2ファイルの読み込み、歌詞の貼り付け |
| 2 | 解析中（Analyzing） | 解析パイプラインの進捗表示・キャンセル |
| 3 | 編集（Editor） | 歌詞タイミング・テキスト・文字送りの手動補正 |
| 4 | 本番（Perform） | カラオケとして再生、字幕演出付き |
| － | 設定（Settings） | アプリ全体の設定。モーダル表示 |

---

## 4. 機能要件

### 4.1 プロジェクト管理

- プロジェクト単位でデータを保存・再読込できること。
- 保存形式は `.dokokara`（単一ファイル。実体は ZIP コンテナ）。詳細は §7。
- 音源ファイルは**プロジェクト内にコピーして格納する**。参照パス方式は採らない（元ファイルの移動・削除でプロジェクトが壊れるため）。
- 対応操作：新規作成 / 開く / 上書き保存 / 名前を付けて保存 / 複製 / リネーム / 削除。
- 未保存の変更がある状態で閉じようとした場合、破棄・保存・キャンセルを選べるダイアログを表示する。
- 一定間隔（既定 3 分）で作業ファイルの自動バックアップを取り、クラッシュ後の復帰時に復元を提案する。

#### 4.1.1 ホーム画面

- 保存済みプロジェクトをカード形式のグリッドで一覧表示する。
- カードに表示する項目：プロジェクト名 / 波形サムネイル / 曲の長さ / 歌詞行数 / 最終更新日時。
- 並び替え：更新日時（既定）/ 作成日時 / 名前。
- プロジェクト名によるインクリメンタル検索。
- 「最近開いたプロジェクト」を先頭に固定表示する。
- 削除時は確認ダイアログを出し、ファイル自体をゴミ箱へ移動する（完全削除はしない）。
- プロジェクトが1件もない場合、空状態のイラストと「新規作成」への導線を表示する。
- `.dokokara` ファイルを Finder からアプリアイコンにドラッグ、またはダブルクリックして開けること（`CFBundleDocumentTypes` に登録）。

### 4.2 設定画面

アプリ全体に適用される設定。プロジェクトごとの設定（オフセット等）とは分離する。

| 設定項目 | 既定値 |
|----------|--------|
| プロジェクトの保存先フォルダ | `~/Documents/どこでもカラオケセット/` |
| 自動バックアップ間隔 | 3 分 |
| スナップの有効／無効 | 有効 |
| スナップ判定距離 | 8 px |
| シーク量（←/→） | 0.5 秒 |
| 大シーク量（Shift+←/→） | 5 秒 |
| 本番画面の既定再生ソース | オフボーカル |
| カウントイン | 有効 |

### 4.3 音源読み込み（2ファイル方式）

本アプリは**解析用と再生用に別々の音源を用いる**。これは v1 からの最大の設計変更である。

| 系統 | 使用ファイル | 用途 |
|------|--------------|------|
| 解析系統 | オンボーカル音源（必須） | ピッチ検出、フレーズ検出、歌詞の自動割り当て、ピッチガイドの生成 |
| 再生系統 | オフボーカル音源（任意） | 編集画面・本番画面での音声再生 |

- オフボーカル音源が未指定の場合は、オンボーカル音源を再生系統にも流用する（練習用途としては成立するため、必須にはしない）。
- 対応形式：**wav / mp3 / m4a(AAC) / flac**。Electron（Chromium）の `decodeAudioData` が対応する範囲とする。
- ファイル選択ダイアログおよびドラッグ&ドロップに対応する。ドロップゾーンは解析用・再生用の2つを並べて配置する。
- 読み込み後、それぞれの波形サムネイルと再生時間を表示する。
- 2ファイルの長さが著しく異なる場合（差が 3 秒以上）、別マスターの可能性がある旨を警告する。
- 数十MB規模のファイルでも UI がフリーズしないこと（デコードは非同期、進捗表示あり）。

### 4.4 解析パイプライン

**方針：処理時間よりも精度を優先する。** 5分の楽曲で 60 秒程度かかることを許容する。ただし進捗表示とキャンセルは必須とする。

処理は以下の順に実行し、各ステップの進捗を画面に表示する。

#### 4.4.1 STEP 1 — 2ファイルの時間軸アライメント

- オンボーカルとオフボーカルの先頭 30 秒程度を用いて相互相関を取り、サンプル単位のズレ量を推定する。
- FFT ベースの相互相関を用いる（時間領域の総当たりは避ける）。
- 推定したズレ量を以降の処理に適用する。
- 相関のピークが不明瞭な場合（別マスターと判断される場合）はアライメント不能とみなし、STEP 2 をスキップする。

#### 4.4.2 STEP 2 — 差分ボーカル抽出

- アライメント済みの2信号を減算し、歌声のみに近い信号を得る。
- 減算後の残差エネルギー比を評価し、**伴奏が十分に打ち消せているかを自動判定する**。判定に失敗した場合は、オンボーカル音源をそのまま解析対象とする通常経路にフォールバックする。
- 抽出結果はユーザーが試聴できること。「うまく抜けているか」を耳で確認し、必要なら手動で通常経路に切り替えられるようにする。
- ステレオの位相差やエンコード差により完全な打ち消しは期待できない。**成功すれば精度が大幅に上がるが、失敗しても動作する**構成とすること。

#### 4.4.3 STEP 3 — 前処理

- モノラル化し、22,050 Hz にリサンプルする（プロトタイプの 11,025 Hz から引き上げ、高音域の分解能を確保する）。
- ハイパスフィルタ（約 60 Hz）で低域の残留成分を除去する。

#### 4.4.4 STEP 4 — ピッチ検出

- **YIN 系のアルゴリズム**を用いる（累積平均正規化差分関数＋絶対閾値＋放物線補間）。プロトタイプの素の自己相関からの置き換えとする。
- 検出範囲：70 Hz 〜 1100 Hz（人の歌声を想定）。
- ホップサイズ：**5 ms**（プロトタイプの約 46 ms から大幅に細かくする。文字単位タイミングの精度がこれに依存する）。
- 後処理：
  - **オクターブ誤り補正** — 1オクターブ跳んで戻る不自然な軌跡を検出し補正する
  - **中央値フィルタ** — 単発の外れ値を除去する
  - **連続性を考慮した平滑化** — フレーム間のピッチ変化にペナルティを課し、最尤の軌跡を選ぶ（Viterbi 的経路探索）
- 有声／無声判定は、YIN の非周期性指標・RMS・（差分ボーカルが有効な場合は）その信号のエネルギーを組み合わせて行う。

#### 4.4.5 STEP 5 — オンセット検出

- スペクトラルフラックスによりオンセット（音の立ち上がり）を検出する。
- 検出結果は「文字境界の候補」および「編集画面でのスナップ先」として保持する。

#### 4.4.6 STEP 6 — フレーズ区間検出

- 有声フレームの連続をフレーズ区間としてまとめる。
- 短い無声を跨いで結合する（既定 0.18 秒以下のギャップは結合）。
- 極端に短い区間（0.12 秒未満）は破棄する。

#### 4.4.7 STEP 7 — 歌詞行への割り当て

- 検出したフレーズ区間数と歌詞行数を突き合わせて、開始・終了時刻を自動割り当てする。
- フレーズ数 > 行数の場合：ギャップが最小の隣接区間から順に統合する。
- フレーズ数 < 行数の場合：最も長い区間から分割する。**分割位置は等分ではなく、対象行の文字数（モーラ数）比で決定する**。
- フレーズが検出できない場合：楽曲全体を行数で均等分割するフォールバックを用意する。

#### 4.4.8 STEP 8 — 文字タイミングの初期配分

- 各行に割り当てた時間を、行内のトークンへ配分する（§4.6.3）。

### 4.5 歌詞入力

- テキストエリアへの直接入力、および外部テキストファイル（.txt）からの読み込みに対応する。
- **1行 = 1フレーズ**として扱う。空行は無視する。
- 行数のリアルタイムカウントを表示する。
- **ルビ記法**をサポートする。以下の2記法を受け付ける。
  - 括弧記法：`今日(きょう)は晴れ`
  - 青空文庫記法：`｜今日《きょう》は晴れ`
- ルビ記法はプレビュー表示で確認できること。
- 入力時に全角／半角スペースを除去するかどうかを選べる。

### 4.6 文字単位タイミング

本アプリの中核機能。カラオケ字幕としての品質はここで決まる。

#### 4.6.1 トークン分割ルール

行を「文字送りの単位（トークン）」に分割する。分割は以下のルールで自動的に行う。

| 対象 | 扱い |
|------|------|
| 基本 | かな・漢字1文字を1トークンとする |
| 拗音（ゃ ゅ ょ ぁぃぅぇぉ 等） | 直前の文字と結合して1トークン（例：`きょ` で1つ） |
| 促音（っ） | 直前のトークンに結合する |
| 長音（ー） | 直前のトークンに結合する |
| 撥音（ん） | 独立した1トークンとする |
| ルビ付き漢字 | ルビの範囲を1トークンとする（例：`今日(きょう)` で1つ） |
| 英単語 | 1単語を1トークンとする（文字単位に割らない） |
| 数字 | 連続する数字列を1トークンとする |
| 記号・空白 | トークンとして扱わず、直前のトークンに含める |

- 自動分割の結果はユーザーが編集できる（トークンの結合・分割を手動で行える）。

#### 4.6.2 データ構造

各行は `tokens[]` を持ち、各トークンは以下を保持する。

- `text` — 表示文字列
- `ruby` — ルビ文字列（無ければ null）
- `start` / `end` — 秒
- `locked` — 自動再配分の対象外にするフラグ

行の `start` / `end` は、先頭トークンの `start` と末尾トークンの `end` に一致する（不整合を持たせない）。

#### 4.6.3 初期配分アルゴリズム

行の時間をトークンへ配分する際、単純な等分は行わない。

1. **重み付き配分** — 各トークンにモーラ重みを与える。促音・長音を含むトークンは重みを増やす。ルビ付き漢字はルビのモーラ数を重みとする。
2. **オンセットへのスナップ** — STEP 5 で検出したオンセット位置が近傍にある場合、トークン境界をそこへ吸着させる。
3. **ピッチ変化点の考慮** — 有意なピッチ変化点も境界候補として扱う。
4. 上記が得られない区間は、重み比で線形配分する。

#### 4.6.4 文字タイミング編集 UI

- 行を選択すると、タイムライン上に**文字境界バー**が展開表示される。
- 境界バーをドラッグして、前後トークンの区切り位置を調整できる（両隣のトークンが同時に伸縮する）。
- トークンを右クリックで「前と結合」「ここで分割」「ロック」を選べる。
- ロックしたトークンは、行全体の移動・再配分の影響を受けない。
- 行の長さを変更したとき、内部トークンは**相対比率を保って伸縮する**（ロック済みを除く）。
- 「この行の文字タイミングを再配分」コマンドで、初期配分アルゴリズムを再適用できる。

#### 4.6.5 タップ入力

再生しながらキー入力でタイミングを打ち込む、最も高速な入力手段。

- タップ入力モードに入ると、再生位置に対して `T` キーで**次のトークンの開始時刻を確定**していく。
- 行の最後のトークンを確定すると、次の行へ自動的に移る。
- `Shift+T` で1つ前のトークンに戻ってやり直せる。
- タップ入力の結果は1操作としてまとめて Undo できる。

#### 4.6.6 塗り（文字送り）の描画仕様

- 現在時刻が属するトークンのみ、内部を左から右へグラデーションで塗る。
- 既に過ぎたトークンは完全に塗り、未到達のトークンは未塗りとする。
- ルビも本体と同じ進捗で塗る。
- トークン内の塗り進行は線形補間とする。

### 4.7 編集画面

#### 4.7.1 表示要素

- 検出したピッチを時間軸上にリボン状のグラフとして描画する。
- 歌詞の各行をタイムライン上のブロックとして表示する。
- 選択中の行は、文字境界バーを展開表示する。
- 検出済みフレーズ区間・オンセット位置を、薄いガイド線として背景に表示する（表示ON/OFF切替可）。
- プレイヘッドをリアルタイム表示し、画面外に出た場合は自動スクロールする。
- 波形（オフボーカル）をピッチリボンの背景に薄く重ねて表示する。

#### 4.7.2 基本操作

- ブロックをドラッグして開始・終了時刻をまとめて移動する。
- ブロック端をドラッグして開始／終了時刻を個別に変更する（隣接ブロックと重ならないよう制限する）。
- ブロックをクリックして選択し、テキスト内容を修正する。
- 開始・終了時刻を数値で直接入力して微調整する。
- タイムラインの拡大・縮小（ズーム）。ズーム率は 0.25 倍〜8 倍。
- 再生・一時停止、タイムライン背景クリックによるシーク。
- 行の追加・削除・分割・結合。
- 再生ソースの切替（オフボーカル／オンボーカル）。タイミング合わせ中は歌が聴こえた方が便利なため。

#### 4.7.3 スナップ機構

ドラッグ操作時、以下の位置に吸着する。

| 優先度 | スナップ先 |
|--------|-----------|
| 1 | プレイヘッド（現在の再生位置） |
| 2 | 隣接ブロックの端 |
| 3 | 検出済みフレーズ区間の境界 |
| 4 | オンセット位置 |
| 5 | 0.1 秒グリッド |

- **判定距離は秒ではなくピクセルで定義する**（既定 8 px）。ズーム率が変わっても操作感が変わらないようにするため。
- 複数の候補が判定距離内にある場合、上表の優先度が高いものを採用する。
- ドラッグ中に **Option キー**を押している間はスナップを無効化する。
- 吸着中は、スナップ先のガイド線をハイライト表示してフィードバックする。
- ブロック移動時は開始端・終了端の両方を判定対象とし、より近い方を吸着させる。
- 文字境界バーのドラッグにも同じ機構を適用する。

### 4.8 Undo / Redo

- 履歴は **100 操作以上**保持する。
- 対象操作：
  - ブロックの移動・リサイズ
  - 文字境界の変更、トークンの結合・分割・ロック
  - 歌詞テキストの編集
  - 行の追加・削除・分割・結合
  - タップ入力の一連の確定
  - 自動割り当ての再実行、文字タイミングの再配分
  - 一括オフセット適用
- **連続したドラッグは1操作にまとめる**（`pointerup` でコミットする）。ドラッグ中の中間状態は履歴に積まない。
- テキスト入力は 500 ms 無操作でコミットする（1文字ずつ履歴に積まない）。
- 保存時点をマークし、Undo で保存時点まで戻った場合は「未変更」状態として扱う。
- 再生位置・ズーム・選択状態は履歴の対象外とする。

### 4.9 キーボードショートカット

#### 4.9.1 共通ルール

- `input` / `textarea` にフォーカスがある間は、再生系・編集系のショートカットを**発火させない**。
- `Space` および矢印キーは `preventDefault()` し、ページスクロールを抑止する。
- ショートカット一覧はアプリ内から `⌘/` で参照できる。

#### 4.9.2 一覧

**ファイル操作**

| キー | 動作 |
|------|------|
| `⌘N` | 新規プロジェクト |
| `⌘O` | プロジェクトを開く |
| `⌘S` | 上書き保存 |
| `⌘⇧S` | 名前を付けて保存 |
| `⌘W` | ウィンドウを閉じる |
| `⌘,` | 設定を開く |

**再生**

| キー | 動作 |
|------|------|
| `Space` | 再生／一時停止 |
| `←` / `→` | 0.5 秒シーク |
| `⇧←` / `⇧→` | 5 秒シーク |
| `⌘←` / `⌘→` | 前／次のブロック先頭へジャンプ |
| `Home` | 先頭へ |
| `Enter`（本番画面） | 最初から再生 |

**選択・編集**

| キー | 動作 |
|------|------|
| `↑` / `↓` | 前／次の行を選択 |
| `Tab` / `⇧Tab` | 前／次のトークンを選択 |
| `Return` | 選択行のテキスト編集へフォーカス |
| `Esc` | 編集を抜ける／選択解除 |
| `I` | 選択行の開始時刻を現在の再生位置に設定 |
| `O` | 選択行の終了時刻を現在の再生位置に設定 |
| `T` | タップ入力：次のトークンを確定 |
| `⇧T` | タップ入力：1つ前に戻る |
| `⌘K` | 選択行を再生位置で分割 |
| `⌘J` | 選択行を次の行と結合 |
| `Delete` | 選択行を削除 |
| `⌘Z` | Undo |
| `⌘⇧Z` | Redo |

**表示**

| キー | 動作 |
|------|------|
| `+` / `-` | ズームイン／アウト |
| `0` | 全体表示 |
| `Option`（押下中） | スナップ一時無効 |
| `⌘Return` | 本番モードへ |

**オフセット調整**

| キー | 動作 |
|------|------|
| `;` | オフセットを 5 ms 早める |
| `'` | オフセットを 5 ms 遅らせる |

### 4.10 本番（カラオケ再生）モード

- ウィンドウ全体（およびフルスクリーン）で現在の歌詞を大きく表示する。
- **文字送り**：§4.6.6 の仕様で歌詞の色が左から塗り変わる。
- **ルビ表示**：ルビ付きトークンは本体の上に読み仮名を表示し、同じ進捗で塗る。
- **前後の行の表示**：直前の行と次の行を薄く表示する。
- **ピッチガイド**：解析済みピッチをスクロール表示し、現在位置を示すマーカーラインを重ねる。
- **カウントイン**：再生開始時、最初の歌い出しまでのカウントを表示する。
- **間奏カウントダウン**：次の行まで一定時間（既定 4 秒）以上ある場合、カウントダウン表示に切り替える。
- **進捗バー**：楽曲全体の再生進捗を表示する。
- **再生ソース切替**：オフボーカル／オンボーカルを再生中に切り替えられる（練習時にお手本を聴くため）。
- **オフセットのリアルタイム調整**：`;` / `'` で調整でき、調整量が画面に一時表示される。
- 操作：再生 / 一時停止 / 最初から / フルスクリーン切替 / 編集画面へワンクリック復帰。
- 一定時間マウス操作がない場合、操作 UI を自動的にフェードアウトさせる。

### 4.11 音ズレオフセット

- オフセット値は**プロジェクトごと**に保持する（`offsetMs`）。
- 正の値は「表示を遅らせる」方向、負の値は「表示を早める」方向とする。
- 適用対象は**表示系のみ**（歌詞の文字送り、ピッチガイド、プレイヘッド）。音声の再生開始時刻は変更しない。
- 編集画面・本番画面の双方から調整でき、再生中でも即座に反映される。
- 調整単位は 5 ms、`Shift` 併用で 50 ms。
- 既定値は 0 ms。Bluetooth 出力時は 100〜300 ms 程度の遅延が生じうるため、この機能は事実上必須である。

### 4.12 エラーハンドリング

- 音源の読み込み・デコードに失敗した場合、原因（非対応形式／ファイル破損／権限）が分かるメッセージを表示する。
- 再生開始に失敗した場合、無音のまま止まらず、エラーを検知して通知する。
- 解析処理が異常終了した場合、途中状態のまま固まらず、どのステップで失敗したかを示して再試行を促す。
- 解析はいつでもキャンセルでき、キャンセル時は準備画面に戻る。
- 差分ボーカル抽出に失敗した場合はエラーとせず、通常経路にフォールバックした旨を通知して処理を続行する。
- プロジェクトファイルが破損している場合、可能な範囲で読み込みを試み、失敗した項目を明示する。

---

## 5. 非機能要件

| 項目 | 要件 |
|------|------|
| 対応 OS | macOS（Apple Silicon / arm64 のみ）。Intel Mac・Windows・Linux は対象外 |
| 配布形態 | DMG。開発機でビルドし直接手渡しする |
| コード署名 | **ad-hoc 署名を必須とする**。Apple Silicon は署名のないバイナリを実行できないため、Developer ID を持たない場合でも `codesign --force --deep --sign -` 相当の処理が必要 |
| 公証（Notarization） | 行わない。Apple Developer Program には加入しない |
| 初回起動手順 | 受け取り側で `xattr -dr com.apple.quarantine <アプリのパス>` を実行するか、システム設定 → プライバシーとセキュリティ →「このまま開く」で許可する。手順書を README として同梱する |
| オフライン動作 | インターネット接続なしで全機能が利用できること。外部 CDN への依存を持たない |
| 解析性能 | 5 分程度の楽曲で 60 秒以内を目安とする。精度を優先し、時間の短縮は最優先事項としない。進捗表示とキャンセルを必須とする |
| UI 応答性 | 解析中・保存中も UI がフリーズしないこと。重い処理はレンダラのメインスレッドで実行しない |
| 再生同期精度 | 文字送りとピッチガイドの表示が、オフセット補正後に音声と ±20 ms 以内で一致すること |
| データ保存先 | ローカルディスク上のユーザー指定フォルダ |
| 多言語対応 | 初期は日本語 UI のみ。文言はコンポーネント化し、将来的な多言語化に備える |
| ログ | 解析パイプラインの各ステップの所要時間と判定結果をログに残し、設定画面から参照できること |

---

## 6. UI デザイン要件

**プロトタイプ（KARAOKE STUDIO）の雰囲気を踏襲すること**を明示的な要件とする。以下のトークンを維持する。

### 6.1 カラーパレット

| 用途 | 値 |
|------|-----|
| 背景 | `#150F26`（濃紫） |
| パネル背景 | `#1F1737` |
| パネル背景（2段目） | `#251C40` |
| 境界線 | `rgba(201, 194, 223, 0.14)` |
| アクセント（プライマリ） | `#F2B705`（ゴールド） |
| アクセント（セカンダリ） | `#EF5D74`（ローズ） |
| テキスト | `#F5F1FF` |
| テキスト（弱） | `#9C93BF` |
| テキスト（最弱） | `#6E6591` |

- 背景には左上にゴールド、右上にローズの放射グラデーションを薄く重ねる。
- ダークテーマのみ。ライトテーマは提供しない。

### 6.2 タイポグラフィ

| 役割 | フォント | 用途 |
|------|----------|------|
| ディスプレイ | Fraunces | 見出し、本番画面の歌詞。強調箇所はイタリック＋ゴールド |
| 本文 | Inter | UI 全般 |
| モノスペース | IBM Plex Mono | タイムコード、数値入力、ラベル、ステップ表示 |

- 日本語歌詞用のフォントを別途選定・同梱する（本番画面での可読性を優先）。

### 6.3 フォントの同梱

- **Google Fonts の CDN 参照を廃止し、フォントファイルをアプリに同梱する**（オフライン要件との整合のため）。
- Fraunces / Inter / IBM Plex Mono はいずれも SIL Open Font License であり、同梱・再配布に問題はない。

### 6.4 その他の視覚要素

- 角丸：カード 14 px、入力要素 6〜10 px。
- プライマリボタン：ゴールドのグラデーション＋グロー影。ホバーで 1 px 浮き上がる。
- ゴーストボタン：透明背景＋境界線。ホバーでゴールドの境界色。
- ステップインジケータ：モノスペース・大文字・ピル形状。現在ステップはゴールド。
- ピッチリボン：太いゴールドのグロー線の上に細い明色線を重ねる二層描画。
- 歌詞ブロック：ゴールドの半透明。選択中はローズ。
- プレイヘッド：白 2 px ＋グロー。本番画面のマーカーラインはローズ。

---

## 7. データ仕様

### 7.1 `.dokokara` ファイル構造

単一ファイル（ZIP コンテナ）とする。内部構造は以下。

```
project.dokokara
├── project.json        … メタデータ・歌詞・トークン・設定
├── pitch.bin           … ピッチデータ（Float32 バイナリ）
├── onsets.bin          … オンセット時刻（Float32 バイナリ）
└── audio/
    ├── vocal.<ext>     … オンボーカル音源（解析用・原本コピー）
    └── off.<ext>       … オフボーカル音源（再生用・原本コピー）
```

- **ピッチデータは JSON に直接書かない。** ホップ 5 ms・5 分の楽曲では 6 万フレームを超え、JSON では数MB規模に膨らむため、バイナリで別ファイルとする。
- ZIP の圧縮レベルは音源部分については 0（無圧縮）とし、保存時間を短縮する。

### 7.2 `project.json`

```json
{
  "version": 2,
  "app": "dokokara",
  "name": "曲名",
  "createdAt": "ISO8601",
  "updatedAt": "ISO8601",

  "audio": {
    "analysis": {
      "originalFileName": "string",
      "path": "audio/vocal.wav",
      "duration": 0.0,
      "sampleRate": 44100
    },
    "playback": {
      "originalFileName": "string",
      "path": "audio/off.wav",
      "duration": 0.0,
      "sampleRate": 44100
    },
    "alignmentOffsetSamples": 0
  },

  "playback": {
    "offsetMs": 0,
    "defaultSource": "playback"
  },

  "analysis": {
    "method": "yin",
    "vocalIsolation": "difference",
    "hopSec": 0.005,
    "frameCount": 0,
    "pitchFile": "pitch.bin",
    "onsetFile": "onsets.bin",
    "phrases": [
      { "start": 0.0, "end": 0.0 }
    ]
  },

  "lyrics": [
    {
      "id": "string",
      "text": "今日は晴れ",
      "start": 0.0,
      "end": 0.0,
      "tokens": [
        {
          "text": "今日",
          "ruby": "きょう",
          "start": 0.0,
          "end": 0.0,
          "locked": false
        }
      ]
    }
  ]
}
```

### 7.3 バイナリファイル

- `pitch.bin` — `Float32Array`。フレーム数分の周波数（Hz）。無声は `0`。時刻は `index * analysis.hopSec` で復元する。
- `onsets.bin` — `Float32Array`。オンセット時刻（秒）の昇順配列。

---

## 8. 技術構成

### 8.1 方式

**Electron** を採用する。

選定理由：

- プロトタイプ（Web Audio API による解析・再生）をほぼそのまま流用でき、移行コストが最小。
- レンダラが Chromium で固定されるため、`decodeAudioData` の対応形式・挙動が開発機とターゲット機で一致する。WKWebView 特有のコーデック差異を考慮しなくてよい。
- `electron-builder` により、DMG 生成・ad-hoc 署名・arm64 単一ターゲットビルドを一括して扱える。
- 実装を Claude Code に委ねる前提であり、エコシステムの情報量が多い方が生成物の品質が安定する。

トレードオフとして、アプリサイズが 150 MB 前後、メモリ使用量が Tauri より大きくなる点を受け入れる。利用者が 2 名であり、配布コストが問題にならないため。

### 8.2 プロセス構成

| プロセス | 責務 |
|----------|------|
| メインプロセス | ウィンドウ管理、ファイル I/O、`.dokokara` の読み書き（ZIP 展開・生成）、ネイティブメニュー、ダイアログ |
| レンダラプロセス | UI 描画、Web Audio による再生、タイムライン描画 |
| Worker（解析） | アライメント、差分抽出、リサンプル、YIN、オンセット検出、セグメント化 |

- レンダラとメインの通信は `contextBridge` 経由の限定的な API のみ公開する（`nodeIntegration` は無効、`contextIsolation` は有効）。
- 解析 Worker は楽曲をチャンク分割し、複数 Worker で並列処理する。進捗はチャンク完了ごとに集約して通知する。
- Worker への音声データ受け渡しは `Transferable`（`ArrayBuffer`）で行い、コピーを避ける。

### 8.3 ビルド

- ターゲット：`--arm64` のみ。universal ビルドは行わない。
- 出力：DMG。
- 署名：Developer ID を用いず、ad-hoc 署名を明示的に実行する。`electron-builder` の設定によっては署名がスキップされ、Apple Silicon 上で起動できないアプリが生成されうるため、ビルド後に署名の有無を検証する手順をビルドスクリプトに含める。
- `.dokokara` を `CFBundleDocumentTypes` に登録し、Finder からの関連付けを有効にする。

---

## 9. バックログ（将来の検討事項）

初期スコープには含めないが、設計上の余地を残しておく項目。

- 歌唱を録音し、後から聴き返せる機能
- ピッチと歌唱の一致度に基づく簡易採点機能
- LRC / 拡張 LRC（文字単位タイミング付き）へのエクスポート・インポート
- 移調（キー変更）・テンポ変更
- 複数プロジェクトのタグ付け・検索
- 機械学習によるボーカル分離の内蔵（オフボーカル音源が用意できない場合の代替）
- ライトテーマ・多言語 UI
- Intel Mac / Windows 対応
