# どこカラv3 Pythonサイドカーの依存関係。
#
# torch と torchaudio はメジャー・マイナーを必ず揃えること。torchaudio は
# 2.11.0 (2026-03-23) を最後にリリースが止まっており、torch はその後も
# 2.12/2.13/2.14 とリリースが続いている。torch だけ最新に上げると
# torchaudio.functional.forced_align (§4.4.7 のフォースドアライメントで使用)
# の互換性が壊れるおそれがあるため、torch 側もこの組に固定する。
torch==2.11.0
torchaudio==2.11.0
transformers==5.17.0

# ノート化(§4.4.4)。activationはRMVPE由来の出力に差し替えて使う方針のため、
# basic_pitch自身のTF/ONNX/CoreMLモデルは使わず、note_creation等の後処理
# ユーティリティだけを使う想定。macOS + Python 3.11では、basic-pitchのPyPI
# メタデータ上どの条件付き依存(tensorflow-macos等)も適用されず、TF/ONNX
# バックエンド無しでインストールされる(importはwarningのみで成功する。
# 実測済み)。バックエンドが必要になった場合は `basic-pitch[onnx]` 等に
# 差し替えを検討する。
basic-pitch==0.4.0

# ボーカル/伴奏分離(§4.4.2 STEP2)。Kimberley Jensen版Mel-Band RoFormer
# チェックポイントの推論のみを提供する軽量パッケージ(MIT)。同梱チェックポイント
# (resources/models/melband-roformer-kim-vocals/)をmodels_dirに直接渡すことで、
# ダウンロード・~/.cache参照を一切行わずオフラインで動作する。
# アーキテクチャ用YAML設定(config_vocals_mel_band_roformer.yaml)はパッケージに
# 同梱されており別途取得不要。
melband-roformer-infer==0.1.5
