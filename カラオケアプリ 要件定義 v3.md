# どこでもカラオケセット 要件定義書 v3

> v2（2ファイル方式・自作 TypeScript 解析）を土台に、**音源処理を学習済みモデルへ全面移行**した版。
> v2 からの主な変更点：**入力を1ファイル／YouTube URL に一本化**、**ボーカル分離・F0 抽出・歌詞アライメントを Python サイドカーへ移管**、**歌詞タイミングの自動付与**、**採点機能の追加**。
> 編集画面・ショートカット体系・Undo/Redo・UI デザイン要件は v2 から変更しない。

---

## 1. 概要

### 1.1 アプリ名

**どこでもカラオケセット**（英字表記: `Dokokara Karaoke Set`）

プロジェクトファイル拡張子: **`.dokokara`**

### 1.2 目的

楽曲（YouTube の URL またはローカルの音声ファイル1本）と歌詞テキストだけを入力として、伴奏音源・メロディデータ・歌詞タイミングを自動生成し、採点付きのカラオケとして再生できるデスクトップアプリケーションを開発する。

### 1.3 背景

v2 では以下を実装済み。

- Electron + TypeScript による5画面構成（ホーム／準備／解析中／編集／本番）
- `.dokokara`（ZIP コンテナ）によるプロジェクト管理とアトミック保存
- 文字（モーラ）単位タイミングの編集エディタ、スナップ、Undo/Redo、ショートカット体系
- Web Audio による再生と字幕演出、曲ごとの音ズレオフセット

v2 の運用で判明した課題は以下。本書はこれらの解決を主眼とする。

| # | 課題 | v3 での対応 |
|---|------|-------------|
| Q-1 | オフボーカル音源をユーザーが自前で用意する必要があり、曲を追加する敷居が高い | ボーカル分離を内蔵し、1ファイルから自動生成（§4.4.3） |
| Q-2 | 自作 YIN によるピッチ検出が、差分抽出に失敗した曲で実用精度に届かない | RMVPE に置き換え（§4.4.4） |
| Q-3 | 歌詞タイミングがフレーズの重み按分による推測で、結局ほぼ手作業になっていた | 音源に対する forced alignment による実測へ置き換え（§4.4.6） |
| Q-4 | 音源ファイルを用意するところから始まるため、思い立ってすぐ作れない | YouTube URL からの取り込みに対応（§4.3） |
| Q-5 | 歌っても上手くなったかどうかが分からない | 採点機能を追加（§4.12） |

### 1.4 想定ユーザー・利用規模

- 開発者本人を含む **2名**。バンドの練習用カラオケを内製する用途。
- 一般配布・ストア公開は行わない。ビルド成果物を直接手渡し（AirDrop / USB / ファイル共有）する。

### 1.5 スコープ外

明確に対象外とする。

- 音声認識（Speech-to-Text）による歌詞の自動書き起こし ※歌詞は必ずユーザーが手入力・貼り付けする
- ピッチカーブ自体を波形単位で描き直す本格的なボーカルエディタ（Melodyne 等相当）
- 楽曲配信・共有・クラウド同期機能。本アプリはローカルでの個人利用を前提とする
- 外部 API・クラウド推論の利用。**全ての解析処理はデバイス内で完結すること**（§8.1）
- Windows / Linux 対応
- Intel Mac（x64）対応
- 自動アップデート機構

> v2 でスコープ外としていた「歌唱の採点機能」「機械学習によるボーカル音源分離」は、v3 でスコープ内に移動した。

---

## 2. 用語集

| 用語 | 説明 |
|------|------|
| ピッチ（音程） | 音の高さ。基本周波数（Hz）で表現される |
| 原曲音源 | ユーザーが入力した、歌の入った音源。全ての解析の起点 |
| ボーカル音源 | 分離モデルが原曲から抽出した歌声のみの音源。**解析専用**に使う |
| 伴奏音源 | 分離モデルが原曲から抽出した伴奏のみの音源。**再生専用**に使う（＝カラオケ音源） |
| F0 | 基本周波数。フレーム単位の連続値として得られる |
| ノート | F0 を音符単位に量子化したもの。音階・開始・長さを持つ。採点の基準となる |
| フレーズ区間 | 発声が検出された連続区間。アライメントの単位 |
| ライン（行） | 歌詞1行。プロジェクト上の最小の編集ブロック単位 |
| トークン | 行を構成する文字送りの単位。日本語は原則1モーラ、英単語は1単語 |
| モーラ読み | 歌詞テキストを発音単位のカナに変換したもの。アライメントの入力になる |
| フォースドアライメント | 音声と、その音声に対応するテキストを突き合わせ、各単位の時刻を確定させる処理 |
| ルビ | 漢字トークンに付与する読み仮名 |
| プレイヘッド | 現在の再生位置を示すタイムライン上のマーカー |
| オフセット | 音声出力遅延を補正するための、表示側の時間シフト量 |
| 入出力遅延 | マイク入力と音声出力の往復にかかる遅延。採点の基準時刻補正に使う（§4.12.2） |
| サイドカー | アプリに同梱され、解析処理を担当する Python プロセス（§8.2） |

---

## 3. 画面構成・遷移

```
        ┌──────────────────────────────────────────┐
        │                                          │
   [0. ホーム] ──新規──> [1. 準備] ──> [2. 解析中] ──> [3. 編集] <──> [4. 本番]
        │                                              │              │
        └────────既存プロジェクトを開く─────────────────┘         [5. リザルト]

   [設定] … いずれの画面からも ⌘, で開けるモーダル
```

| # | 画面名 | 役割 |
|---|--------|------|
| 0 | ホーム（Home） | プロジェクト一覧・新規作成・開く・削除・複製 |
| 1 | 準備（Setup） | YouTube URL または音声ファイル1本の指定、歌詞の貼り付け |
| 2 | 解析中（Analyzing） | 解析パイプラインの進捗表示・キャンセル・バックグラウンド化 |
| 3 | 編集（Editor） | 歌詞タイミング・テキスト・文字送りの手動補正 |
| 4 | 本番（Perform） | カラオケとして再生、字幕演出＋採点 |
| 5 | リザルト（Result） | 歌い終わりの採点結果表示 |
| － | 設定（Settings） | アプリ全体の設定。モーダル表示 |

---

## 4. 機能要件

### 4.1 プロジェクト管理

- プロジェクト単位でデータを保存・再読込できること。
- 保存形式は `.dokokara`（単一ファイル。実体は ZIP コンテナ）。詳細は §7。
- 音源ファイルは**プロジェクト内にコピーして格納する**。参照パス方式は採らない（元ファイルの移動・削除でプロジェクトが壊れるため）。
- 対応操作：新規作成 / 開く / 上書き保存 / 名前を付けて保存 / 複製 / リネーム / 削除。
- 未保存の変更がある状態で閉じようとした場合、破棄・保存・キャンセルを選べるダイアログを表示する。
- 一定間隔（既定 3 分）で作業ファイルの自動バックアップを取り、クラッシュ後の復帰時に復元を提案する。

#### 4.1.1 ホーム画面

- 保存済みプロジェクトをカード形式のグリッドで一覧表示する。
- カードに表示する項目：プロジェクト名 / 波形サムネイル / 曲の長さ / 歌詞行数 / 最終更新日時。
- 並び替え：更新日時（既定）/ 作成日時 / 名前。
- プロジェクト名によるインクリメンタル検索。
- 「最近開いたプロジェクト」を先頭に固定表示する。
- 削除時は確認ダイアログを出し、ファイル自体をゴミ箱へ移動する（完全削除はしない）。
- プロジェクトが1件もない場合、空状態のイラストと「新規作成」への導線を表示する。
- `.dokokara` ファイルを Finder からアプリアイコンにドラッグ、またはダブルクリックして開けること（`CFBundleDocumentTypes` に登録）。

### 4.2 設定画面

アプリ全体に適用される設定。プロジェクトごとの設定（オフセット等）とは分離する。

| 設定項目 | 既定値 |
|----------|--------|
| プロジェクトの保存先フォルダ | `~/Documents/どこでもカラオケセット/` |
| 自動バックアップ間隔 | 3 分 |
| スナップの有効／無効 | 有効 |
| スナップ判定距離 | 8 px |
| シーク量（←/→） | 0.5 秒 |
| 大シーク量（Shift+←/→） | 5 秒 |
| 本番画面の既定再生ソース | 伴奏音源 |
| カウントイン | 有効 |

### 4.3 音源の取り込み（1入力方式）

v2 の2ファイル方式（オンボーカル＋オフボーカル）は**廃止する**。ユーザーが与えるのは歌の入った音源1本のみで、伴奏音源はアプリが生成する。

| 入力方法 | 内容 |
|----------|------|
| YouTube URL | URL を貼り付けると、同梱の yt-dlp で音声のみを取得する |
| ローカルファイル | ファイル選択ダイアログまたはドラッグ&ドロップ |

- 対応形式：**wav / mp3 / m4a(AAC) / flac**。
- ドロップゾーンは1つに統合する。URL 入力欄とドロップゾーンを同一画面に並置し、どちらか一方が埋まれば次へ進める。
- 取り込み後、波形サムネイルと再生時間を表示する。
- YouTube からの取得は以下を明示的にハンドリングする。いずれも準備画面上でユーザーに理由を提示し、ローカルファイル入力へ誘導する。
  - 動画が非公開・削除済み
  - 地域制限・年齢制限
  - yt-dlp の仕様変更による取得失敗
- 取得中は進捗（ダウンロード率）を表示し、キャンセルできること。
- 取り込んだ音源は原曲音源としてプロジェクトに保持し、以降の全ステップの入力とする。

### 4.4 解析パイプライン

**方針：処理時間よりも精度を優先する。** 4〜5分の楽曲で **5〜10 分**かかることを許容する。ただし進捗表示・キャンセル・バックグラウンド化を必須とする。

全ステップを Python サイドカー（§8.2）で実行する。v2 のレンダラ内 Worker 方式は廃止する。

| STEP | 処理 | 使用技術 | 想定所要時間（4分の曲 / M1） |
|------|------|----------|------------------------------|
| 1 | 音源取得 | yt-dlp + ffmpeg | 10〜60 秒（URL 入力時のみ） |
| 2 | ボーカル／伴奏分離 | Mel-Band RoFormer | 2〜5 分 |
| 3 | F0 抽出 | RMVPE | 30〜60 秒 |
| 4 | ノート化 | Basic Pitch（activation を RMVPE 由来に差し替え） | 10〜30 秒 |
| 5 | フレーズ区間検出 | Silero VAD | 数秒 |
| 6 | 歌詞のモーラ読み変換 | pyopenjtalk | 数秒 |
| 7 | フォースドアライメント | wav2vec2 + CTC | 1〜2 分 |

#### 4.4.1 STEP 1 — 音源取得

- URL 入力の場合、yt-dlp で音声ストリームのみを取得し、ffmpeg で wav（44.1 kHz / ステレオ）に正規化する。
- ローカルファイルの場合も同じく wav に正規化してから後続へ渡す。デコード差異を後段に持ち込まない。

#### 4.4.2 STEP 2 — ボーカル／伴奏分離

- Mel-Band RoFormer（Kim 版チェックポイント）で原曲を vocals / instrumental の2ステムに分離する。
- 8 秒チャンク＋オーバーラップで処理するため、チャンク単位で進捗率を算出できる。これをそのまま進捗表示に使う。
- 生成物の用途は以下。
  - **vocals** … STEP 3・5・7 の入力。解析専用
  - **instrumental** … 本番画面・編集画面での再生音源
- 分離が明らかに失敗した場合（vocals がほぼ無音、または instrumental に歌が残る）、ユーザーに警告したうえで原曲音源での続行を選べるようにする。RMVPE は伴奏混じりでも動作するため、解析自体は継続できる。

#### 4.4.3 STEP 3 — F0 抽出

- 分離済み vocals に対して RMVPE を適用し、フレーム単位の F0（Hz）を得る。
- 出力フレームレートは **100 fps（hop 10 ms）** とする。v2 の 5 ms から変更となるため、`pitch.bin` の仕様を合わせる（§7.3）。
- 無声フレームは `0` とする。
- v2 で実装していたオクターブ補正・メディアンフィルタ・軌跡平滑化は行わない。RMVPE の出力をそのまま採用する。

#### 4.4.4 STEP 4 — ノート化

- 連続値の F0 は採点の基準として使えないため、音符単位に量子化してノート列を生成する。
- Basic Pitch を用い、その multi-pitch activation を RMVPE 由来の出力に差し替える構成とする。
- ビブラート・しゃくり・ポルタメントを1ノートに潰しすぎないよう、ノート境界の閾値は調整可能なパラメータとして持つ。
- 生成したノート列は `notes` として保存し（§7.2）、本番画面のピッチガイドと採点の基準の両方に使う。

#### 4.4.5 STEP 5 — フレーズ区間検出

- 分離済み vocals に Silero VAD を適用し、発声区間を検出する。
- 検出した区間を STEP 7 のアライメント単位とする。1曲まるごとを一括でアライメントすると処理が破綻するため、この分割は必須とする。
- v2 の `phrase.ts`（F0 の有声区間からフレーズを切る方式）の置き換えにあたる。

#### 4.4.6 STEP 6 — 歌詞のモーラ読み変換

- 入力された歌詞テキストを pyopenjtalk でモーラ読み（カナ）に変換する。
- ユーザーが `｜漢字《かんじ》` 記法でルビを明示している場合は、**常にそちらを優先する**。
- 同形異音語（例：「河野」→ コーノ／カワノ）は複数の読み候補を生成し、STEP 7 の CTC 尤度が最も高い候補を採用する。

#### 4.4.7 STEP 7 — フォースドアライメント

- 日本語 wav2vec2 のフレーム確率と、STEP 6 のモーラ読みを入力として CTC forced alignment を実行し、モーラ単位の開始・終了時刻を確定する。
- STEP 5 で区切ったフレーズ区間ごとに実行する。
- 出力はそのまま `lyrics[].tokens[]` の `start` / `end` に流し込む。v2 のようなフレーズ重み按分（`assign.ts`）・行内線形配分（`allocate.ts`）は行わない。
- 各行についてアライメントの**信頼度**を算出し、プロジェクトに保持する。信頼度が閾値を下回る行は編集画面で視覚的に区別し、ユーザーが優先的に確認できるようにする（§4.7.1）。
- 完全自動を目標とはしない。**自動で付与した結果を手で直せる状態にすることを到達点とする。**

### 4.5 歌詞入力

- テキストエリアへの直接入力、および外部テキストファイル（.txt）からの読み込みに対応する。
- **1行 = 1フレーズ**として扱う。空行は無視する。
- 行数のリアルタイムカウントを表示する。
- **ルビ記法**をサポートする。以下の2記法を受け付ける。
  - 括弧記法：`今日(きょう)は晴れ`
  - 青空文庫記法：`｜今日《きょう》は晴れ`
- ルビ記法はプレビュー表示で確認できること。
- 入力時に全角／半角スペースを除去するかどうかを選べる。

### 4.6 文字単位タイミング

本アプリの中核機能。カラオケ字幕としての品質はここで決まる。

#### 4.6.1 トークン分割ルール

行を「文字送りの単位（トークン）」に分割する。分割は以下のルールで自動的に行う。

| 対象 | 扱い |
|------|------|
| 基本 | かな・漢字1文字を1トークンとする |
| 拗音（ゃ ゅ ょ ぁぃぅぇぉ 等） | 直前の文字と結合して1トークン（例：`きょ` で1つ） |
| 促音（っ） | 直前のトークンに結合する |
| 長音（ー） | 直前のトークンに結合する |
| 撥音（ん） | 独立した1トークンとする |
| ルビ付き漢字 | ルビの範囲を1トークンとする（例：`今日(きょう)` で1つ） |
| 英単語 | 1単語を1トークンとする（文字単位に割らない） |
| 数字 | 連続する数字列を1トークンとする |
| 記号・空白 | トークンとして扱わず、直前のトークンに含める |

- 自動分割の結果はユーザーが編集できる（トークンの結合・分割を手動で行える）。

#### 4.6.2 データ構造

各行は `tokens[]` を持ち、各トークンは以下を保持する。

- `text` — 表示文字列
- `ruby` — ルビ文字列（無ければ null）
- `start` / `end` — 秒
- `locked` — 自動再配分の対象外にするフラグ

行の `start` / `end` は、先頭トークンの `start` と末尾トークンの `end` に一致する（不整合を持たせない）。

#### 4.6.3 初期タイミングの決定

v2 の「行の尺をトークンの重みで按分する」初期配分アルゴリズムは**廃止する**。

- トークンの `start` / `end` は §4.4.7 のフォースドアライメント結果を直接採用する。推測による配分は行わない。
- アライメント結果とトークン列の対応が取れなかった行（信頼度が閾値未満、またはモーラ数が一致しない行）に限り、フォールバックとして行内を等分配分し、要確認としてマークする。
- ユーザーが編集画面で行の開始・終了をドラッグして直した場合、その行のトークンを尺に合わせて再配分する。これは v2 の配分ロジックを編集機能として残したものであり、解析パイプラインの一部ではない。

#### 4.6.4 文字タイミング編集 UI

- 行を選択すると、タイムライン上に**文字境界バー**が展開表示される。
- 境界バーをドラッグして、前後トークンの区切り位置を調整できる（両隣のトークンが同時に伸縮する）。
- トークンを右クリックで「前と結合」「ここで分割」「ロック」を選べる。
- ロックしたトークンは、行全体の移動・再配分の影響を受けない。
- 行の長さを変更したとき、内部トークンは**相対比率を保って伸縮する**（ロック済みを除く）。
- 「この行の文字タイミングを再配分」コマンドで、初期配分アルゴリズムを再適用できる。

#### 4.6.5 タップ入力

再生しながらキー入力でタイミングを打ち込む、最も高速な入力手段。

- タップ入力モードに入ると、再生位置に対して `T` キーで**次のトークンの開始時刻を確定**していく。
- 行の最後のトークンを確定すると、次の行へ自動的に移る。
- `Shift+T` で1つ前のトークンに戻ってやり直せる。
- タップ入力の結果は1操作としてまとめて Undo できる。

#### 4.6.6 塗り（文字送り）の描画仕様

- 現在時刻が属するトークンのみ、内部を左から右へグラデーションで塗る。
- 既に過ぎたトークンは完全に塗り、未到達のトークンは未塗りとする。
- ルビも本体と同じ進捗で塗る。
- トークン内の塗り進行は線形補間とする。

### 4.7 編集画面

#### 4.7.1 表示要素

- 検出したピッチを時間軸上にリボン状のグラフとして描画する。
- 歌詞の各行をタイムライン上のブロックとして表示する。
- 選択中の行は、文字境界バーを展開表示する。
- 検出済みフレーズ区間・オンセット位置を、薄いガイド線として背景に表示する（表示ON/OFF切替可）。
- プレイヘッドをリアルタイム表示し、画面外に出た場合は自動スクロールする。
- 波形（伴奏音源）をピッチリボンの背景に薄く重ねて表示する。
- アライメント信頼度が閾値を下回る行は、歌詞ブロックを要確認スタイル（枠線をローズ）で区別する（§4.4.7）。

#### 4.7.2 基本操作

- ブロックをドラッグして開始・終了時刻をまとめて移動する。
- ブロック端をドラッグして開始／終了時刻を個別に変更する（隣接ブロックと重ならないよう制限する）。
- ブロックをクリックして選択し、テキスト内容を修正する。
- 開始・終了時刻を数値で直接入力して微調整する。
- タイムラインの拡大・縮小（ズーム）。ズーム率は 0.25 倍〜8 倍。
- 再生・一時停止、タイムライン背景クリックによるシーク。
- 行の追加・削除・分割・結合。
- 再生ソースの切替（伴奏音源／原曲音源）。タイミング合わせ中は歌が聴こえた方が便利なため。

#### 4.7.3 スナップ機構

ドラッグ操作時、以下の位置に吸着する。

| 優先度 | スナップ先 |
|--------|-----------|
| 1 | プレイヘッド（現在の再生位置） |
| 2 | 隣接ブロックの端 |
| 3 | 検出済みフレーズ区間の境界 |
| 4 | オンセット位置 |
| 5 | 0.1 秒グリッド |

- **判定距離は秒ではなくピクセルで定義する**（既定 8 px）。ズーム率が変わっても操作感が変わらないようにするため。
- 複数の候補が判定距離内にある場合、上表の優先度が高いものを採用する。
- ドラッグ中に **Option キー**を押している間はスナップを無効化する。
- 吸着中は、スナップ先のガイド線をハイライト表示してフィードバックする。
- ブロック移動時は開始端・終了端の両方を判定対象とし、より近い方を吸着させる。
- 文字境界バーのドラッグにも同じ機構を適用する。

### 4.8 Undo / Redo

- 履歴は **100 操作以上**保持する。
- 対象操作：
  - ブロックの移動・リサイズ
  - 文字境界の変更、トークンの結合・分割・ロック
  - 歌詞テキストの編集
  - 行の追加・削除・分割・結合
  - タップ入力の一連の確定
  - 自動割り当ての再実行、文字タイミングの再配分
  - 一括オフセット適用
- **連続したドラッグは1操作にまとめる**（`pointerup` でコミットする）。ドラッグ中の中間状態は履歴に積まない。
- テキスト入力は 500 ms 無操作でコミットする（1文字ずつ履歴に積まない）。
- 保存時点をマークし、Undo で保存時点まで戻った場合は「未変更」状態として扱う。
- 再生位置・ズーム・選択状態は履歴の対象外とする。

### 4.9 キーボードショートカット

#### 4.9.1 共通ルール

- `input` / `textarea` にフォーカスがある間は、再生系・編集系のショートカットを**発火させない**。
- `Space` および矢印キーは `preventDefault()` し、ページスクロールを抑止する。
- ショートカット一覧はアプリ内から `⌘/` で参照できる。

#### 4.9.2 一覧

**ファイル操作**

| キー | 動作 |
|------|------|
| `⌘N` | 新規プロジェクト |
| `⌘O` | プロジェクトを開く |
| `⌘S` | 上書き保存 |
| `⌘⇧S` | 名前を付けて保存 |
| `⌘W` | ウィンドウを閉じる |
| `⌘,` | 設定を開く |

**再生**

| キー | 動作 |
|------|------|
| `Space` | 再生／一時停止 |
| `←` / `→` | 0.5 秒シーク |
| `⇧←` / `⇧→` | 5 秒シーク |
| `⌘←` / `⌘→` | 前／次のブロック先頭へジャンプ |
| `Home` | 先頭へ |
| `Enter`（本番画面） | 最初から再生 |

**選択・編集**

| キー | 動作 |
|------|------|
| `↑` / `↓` | 前／次の行を選択 |
| `Tab` / `⇧Tab` | 前／次のトークンを選択 |
| `Return` | 選択行のテキスト編集へフォーカス |
| `Esc` | 編集を抜ける／選択解除 |
| `I` | 選択行の開始時刻を現在の再生位置に設定 |
| `O` | 選択行の終了時刻を現在の再生位置に設定 |
| `T` | タップ入力：次のトークンを確定 |
| `⇧T` | タップ入力：1つ前に戻る |
| `⌘K` | 選択行を再生位置で分割 |
| `⌘J` | 選択行を次の行と結合 |
| `Delete` | 選択行を削除 |
| `⌘Z` | Undo |
| `⌘⇧Z` | Redo |

**表示**

| キー | 動作 |
|------|------|
| `+` / `-` | ズームイン／アウト |
| `0` | 全体表示 |
| `Option`（押下中） | スナップ一時無効 |
| `⌘Return` | 本番モードへ |

**オフセット調整**

| キー | 動作 |
|------|------|
| `;` | オフセットを 5 ms 早める |
| `'` | オフセットを 5 ms 遅らせる |

### 4.10 本番（カラオケ再生）モード

- ウィンドウ全体（およびフルスクリーン）で現在の歌詞を大きく表示する。
- **文字送り**：§4.6.6 の仕様で歌詞の色が左から塗り変わる。
- **ルビ表示**：ルビ付きトークンは本体の上に読み仮名を表示し、同じ進捗で塗る。
- **前後の行の表示**：直前の行と次の行を薄く表示する。
- **ピッチガイド**：解析済みピッチをスクロール表示し、現在位置を示すマーカーラインを重ねる。
- **カウントイン**：再生開始時、最初の歌い出しまでのカウントを表示する。
- **間奏カウントダウン**：次の行まで一定時間（既定 4 秒）以上ある場合、カウントダウン表示に切り替える。
- **進捗バー**：楽曲全体の再生進捗を表示する。
- **再生ソース切替**：伴奏音源／原曲音源を再生中に切り替えられる（練習時にお手本を聴くため）。
- **オフセットのリアルタイム調整**：`;` / `'` で調整でき、調整量が画面に一時表示される。
- 操作：再生 / 一時停止 / 最初から / フルスクリーン切替 / 編集画面へワンクリック復帰。
- 一定時間マウス操作がない場合、操作 UI を自動的にフェードアウトさせる。

### 4.11 音ズレオフセット

- オフセット値は**プロジェクトごと**に保持する（`offsetMs`）。
- 正の値は「表示を遅らせる」方向、負の値は「表示を早める」方向とする。
- 適用対象は**表示系のみ**（歌詞の文字送り、ピッチガイド、プレイヘッド）。音声の再生開始時刻は変更しない。
- 編集画面・本番画面の双方から調整でき、再生中でも即座に反映される。
- 調整単位は 5 ms、`Shift` 併用で 50 ms。
- 既定値は 0 ms。Bluetooth 出力時は 100〜300 ms 程度の遅延が生じうるため、この機能は事実上必須である。

### 4.12 採点

v2 ではスコープ外としていた機能。本番画面に統合する。

#### 4.12.1 リアルタイムピッチ検出

- `getUserMedia` でマイク入力を取得し、AudioWorklet 上で歌唱の F0 をリアルタイム検出する。
- 検出手法は **YIN / pYIN 程度の軽量なもの**とする。RMVPE はオフライン解析専用であり、ここでは使用しない。採点の質を決めるのは検出精度ではなく §4.12.2 の遅延補正であるため、リアルタイム側に重いモデルを持ち込まない。
- 設定画面に入力デバイス選択と入力レベルメーターを設ける。
- マイク権限が拒否されている場合は、採点なしで本番再生を続行できること。

#### 4.12.2 入出力の遅延補正

- `AudioContext.outputLatency` と入力デバイス遅延を合算し、歌唱ピッチの時刻を補正したうえでノートと突き合わせる。
- **§4.11 の曲ごとの音ズレオフセット（`playback.offsetMs`）とは別の値として保持する。** 前者は字幕表示のための曲単位の調整、後者は採点のための環境単位の調整であり、混同すると両方が狂う。
- イヤホンとスピーカーで値が変わるため、キャリブレーション手順を設ける。アプリから短いクリック音を再生し、マイクで拾い直して往復遅延を実測する方式とする。
- キャリブレーション結果はアプリ設定（プロジェクトではなく）に保存する。

#### 4.12.3 採点ロジック

- §4.4.4 のノート列を基準とし、歌唱 F0 との一致度から音程正確率を算出する。
- オクターブ違いは**一致として扱う**。原曲キーと歌い手の音域が男女差などで異なるのが常態であり、不一致とすると実用にならない。
- 無声区間（ノートが存在しない時間）の発声は減点対象としない。
- ビブラート・しゃくり・こぶし等の加点要素は初期スコープに含めない。バックログとする。
- スコアは 0〜100 で表示する。

#### 4.12.4 表示

- 本番画面の既存のピッチストリップ（お手本メロディ）に、歌唱ピッチをリアルタイムで重ねて描画する。
- 歌い終わりにリザルト画面へ遷移し、総合点と区間別の内訳を表示する。
- リザルトはプロジェクトに保存せず、その場限りの表示とする（履歴機能はバックログ）。

### 4.13 エラーハンドリング

- 音源の読み込み・デコードに失敗した場合、原因（非対応形式／ファイル破損／権限）が分かるメッセージを表示する。
- 再生開始に失敗した場合、無音のまま止まらず、エラーを検知して通知する。
- 解析処理が異常終了した場合、途中状態のまま固まらず、どのステップで失敗したかを示して再試行を促す。
- 解析はいつでもキャンセルでき、キャンセル時は準備画面に戻る。
- ボーカル分離に失敗した場合はエラーとせず、原曲音源を解析に用いる旨を通知して処理を続行する（`analysis.separationFallback` に記録する）。
- Python サイドカーが起動しない・異常終了した場合は、原因を提示したうえで一時ファイルを掃除し、プロジェクトを壊さずに準備画面へ戻す。
- マイクの権限が拒否されている場合は、採点なしで本番再生を続行できるようにする。
- プロジェクトファイルが破損している場合、可能な範囲で読み込みを試み、失敗した項目を明示する。

---


---

## 5. 非機能要件

| 項目 | 要件 |
|------|------|
| 対応 OS | macOS（Apple Silicon / arm64 のみ）。Intel Mac・Windows・Linux は対象外 |
| 配布形態 | DMG。開発機でビルドし直接手渡しする |
| アプリサイズ | **2.5〜3.5 GB を許容する**。モデル重みと Python 実行環境を全て同梱するため（§8.3）。サイズ削減は最適化項目であって要件ではない |
| コード署名 | **ad-hoc 署名を必須とする**。Apple Silicon は署名のないバイナリを実行できない。Electron 本体だけでなく、**同梱する Python 実行ファイル・ffmpeg・yt-dlp などの実行バイナリ全てが対象**となる |
| 公証（Notarization） | 行わない。Apple Developer Program には加入しない |
| 初回起動手順 | 受け取り側で `xattr -dr com.apple.quarantine <アプリのパス>` を実行するか、システム設定 → プライバシーとセキュリティ →「このまま開く」で許可する。手順書を README として同梱する |
| オフライン動作 | **インターネット接続なしで全機能が利用できること**（YouTube 取り込みを除く）。モデル重みはアプリ同梱分のみを参照し、`~/.cache` 等の外部キャッシュや Hugging Face Hub を実行時に参照しないこと |
| 外部サービス依存 | 推論 API・従量課金サービスに依存しないこと。将来のサービス終了・価格改定でアプリが機能不全に陥らないことを要件とする |
| 解析性能 | 4〜5 分の楽曲で 5〜10 分を目安とする。精度を優先し、時間短縮は最優先事項としない |
| 解析中の操作性 | 解析をバックグラウンドで継続でき、その間に別プロジェクトを開いて編集できること。進捗表示・キャンセルは必須 |
| UI 応答性 | 解析中・保存中も UI がフリーズしないこと。解析はレンダラ側で一切実行しない |
| 再生同期精度 | 文字送りとピッチガイドの表示が、オフセット補正後に音声と ±20 ms 以内で一致すること |
| 採点精度 | 遅延補正後、歌唱ピッチの時刻がノートと ±30 ms 以内で対応すること |
| データ保存先 | ローカルディスク上のユーザー指定フォルダ |
| 多言語対応 | 初期は日本語 UI のみ。文言はコンポーネント化し、将来的な多言語化に備える |
| ログ | 解析パイプラインの各ステップの所要時間と判定結果をログに残し、設定画面から参照できること。サイドカー側の標準エラー出力も取り込むこと |

---

---

## 6. UI デザイン要件

**プロトタイプ（KARAOKE STUDIO）の雰囲気を踏襲すること**を明示的な要件とする。以下のトークンを維持する。

### 6.1 カラーパレット

| 用途 | 値 |
|------|-----|
| 背景 | `#150F26`（濃紫） |
| パネル背景 | `#1F1737` |
| パネル背景（2段目） | `#251C40` |
| 境界線 | `rgba(201, 194, 223, 0.14)` |
| アクセント（プライマリ） | `#F2B705`（ゴールド） |
| アクセント（セカンダリ） | `#EF5D74`（ローズ） |
| テキスト | `#F5F1FF` |
| テキスト（弱） | `#9C93BF` |
| テキスト（最弱） | `#6E6591` |

- 背景には左上にゴールド、右上にローズの放射グラデーションを薄く重ねる。
- ダークテーマのみ。ライトテーマは提供しない。

### 6.2 タイポグラフィ

| 役割 | フォント | 用途 |
|------|----------|------|
| ディスプレイ | Fraunces | 見出し、本番画面の歌詞。強調箇所はイタリック＋ゴールド |
| 本文 | Inter | UI 全般 |
| モノスペース | IBM Plex Mono | タイムコード、数値入力、ラベル、ステップ表示 |

- 日本語歌詞用のフォントを別途選定・同梱する（本番画面での可読性を優先）。

### 6.3 フォントの同梱

- **Google Fonts の CDN 参照を廃止し、フォントファイルをアプリに同梱する**（オフライン要件との整合のため）。
- Fraunces / Inter / IBM Plex Mono はいずれも SIL Open Font License であり、同梱・再配布に問題はない。

### 6.4 その他の視覚要素

- 角丸：カード 14 px、入力要素 6〜10 px。
- プライマリボタン：ゴールドのグラデーション＋グロー影。ホバーで 1 px 浮き上がる。
- ゴーストボタン：透明背景＋境界線。ホバーでゴールドの境界色。
- ステップインジケータ：モノスペース・大文字・ピル形状。現在ステップはゴールド。
- ピッチリボン：太いゴールドのグロー線の上に細い明色線を重ねる二層描画。
- 歌詞ブロック：ゴールドの半透明。選択中はローズ。
- プレイヘッド：白 2 px ＋グロー。本番画面のマーカーラインはローズ。

---

## 7. データ仕様

### 7.1 `.dokokara` ファイル構造

単一ファイル（ZIP コンテナ）とする。内部構造は以下。

```
project.dokokara
├── project.json          … メタデータ・歌詞・トークン・ノート・設定
├── pitch.bin             … F0 データ（Float32 バイナリ）
└── audio/
    ├── source.<ext>      … 原曲音源（入力の原本コピー）
    └── instrumental.<ext>… 伴奏音源（分離で生成・再生用）
```

- **vocals（分離したボーカル音源）は保存しない。** 解析が終われば不要であり、保存するとファイルが1.5倍に膨らむ。再解析が必要な場合は原曲から作り直す。
- v2 の `onsets.bin` は廃止する。オンセット検出をやめ、ノート列が役割を引き継ぐため。
- ノート列は件数が少ない（1曲で数百件程度）ため、バイナリ化せず `project.json` に直接持つ。
- F0 データは 100 fps・5分で3万フレームとなるため、引き続きバイナリで別ファイルとする。
- ZIP の圧縮レベルは音源部分については 0（無圧縮）とし、保存時間を短縮する。

### 7.2 `project.json`

```json
{
  "version": 3,
  "app": "dokokara",
  "name": "曲名",
  "createdAt": "ISO8601",
  "updatedAt": "ISO8601",

  "source": {
    "kind": "youtube",
    "url": "https://...",
    "originalFileName": null
  },

  "audio": {
    "source": {
      "path": "audio/source.wav",
      "duration": 0.0,
      "sampleRate": 44100
    },
    "instrumental": {
      "path": "audio/instrumental.wav",
      "duration": 0.0,
      "sampleRate": 44100
    }
  },

  "playback": {
    "offsetMs": 0
  },

  "analysis": {
    "separation": "melband-roformer-kim",
    "pitchMethod": "rmvpe",
    "alignMethod": "wav2vec2-ctc",
    "separationFallback": false,
    "hopSec": 0.01,
    "frameCount": 0,
    "pitchFile": "pitch.bin",
    "phrases": [
      { "start": 0.0, "end": 0.0 }
    ],
    "notes": [
      { "start": 0.0, "end": 0.0, "midi": 60 }
    ]
  },

  "lyrics": [
    {
      "id": "string",
      "text": "今日は晴れ",
      "start": 0.0,
      "end": 0.0,
      "confidence": 0.0,
      "tokens": [
        {
          "text": "今日",
          "ruby": "きょう",
          "start": 0.0,
          "end": 0.0,
          "locked": false
        }
      ]
    }
  ]
}
```

v2 からの変更点は以下。

| 項目 | v2 | v3 |
|------|----|----|
| `version` | `2` | `3` |
| `source` | なし | 新設。入力の出所（YouTube URL / ローカルファイル名）を記録 |
| `audio.analysis` / `audio.playback` | オンボーカル／オフボーカル | `audio.source` / `audio.instrumental` に置換 |
| `audio.alignmentOffsetSamples` | あり | **削除**。2ファイルの時間軸合わせが不要になったため |
| `playback.defaultSource` | あり | **削除**。再生系統が伴奏音源に一本化されたため |
| `analysis.method` | `"yin"` | `separation` / `pitchMethod` / `alignMethod` に分割 |
| `analysis.vocalIsolation` | `"difference"` / `"none"` | **削除**。`separationFallback` が分離失敗時の続行を記録する |
| `analysis.hopSec` | `0.005` | `0.01`（RMVPE の 100 fps に合わせる） |
| `analysis.onsetFile` | `"onsets.bin"` | **削除** |
| `analysis.notes` | なし | 新設。採点とピッチガイドの基準 |
| `lyrics[].confidence` | なし | 新設。アライメント信頼度 |

**v2 プロジェクトの互換性**：読み込みは行わない。利用者が2名かつ既存プロジェクト数が少ないため、マイグレーションのコストに見合わない。v2 ファイルを開こうとした場合は、非対応である旨を明示して原曲から作り直すよう案内する。

### 7.3 バイナリファイル

- `pitch.bin` — `Float32Array`。フレーム数分の F0（Hz）。無声は `0`。時刻は `index * analysis.hopSec`（= `index * 0.01`）で復元する。

---

## 8. 技術構成

### 8.1 方式

**Electron（UI）＋ Python サイドカー（解析）** のハイブリッド構成とする。

v2 は解析を含めて全て TypeScript で実装していたが、v3 で採用する学習済みモデルは全て Python エコシステム上にあるため、解析層を分離する。

選定理由：

- 必要なモデル（Mel-Band RoFormer / RMVPE / Basic Pitch / wav2vec2 / Silero VAD）が全て Python 実装で入手でき、いずれもオープンウェイトであるため**デバイス内で完結**できる。外部 API に依存しない（§5）。
- UI 側（編集画面・ショートカット体系・Undo/Redo・ZIP 入出力）は v2 の実装が完成しており、Electron を維持すれば全て流用できる。
- Core ML / MLX に寄せれば PyTorch を排して 1〜1.5 GB に収められるが、日本語 wav2vec2 など変換済みモデルが存在しないものを自前で変換する必要がある。**まず PyTorch で組み、速度に不満が出たら分離処理だけ Core ML 化する**方針とする。

### 8.2 プロセス構成

| プロセス | 責務 |
|----------|------|
| メインプロセス | ウィンドウ管理、ファイル I/O、`.dokokara` の読み書き（ZIP 展開・生成）、ネイティブメニュー、ダイアログ、**サイドカーの起動・監視・終了** |
| レンダラプロセス | UI 描画、Web Audio による再生、タイムライン描画、**リアルタイムピッチ検出（AudioWorklet）** |
| Python サイドカー | 音源取得、ボーカル分離、F0 抽出、ノート化、VAD、読み変換、フォースドアライメント |

- レンダラとメインの通信は `contextBridge` 経由の限定的な API のみ公開する（`nodeIntegration` は無効、`contextIsolation` は有効）。
- メインとサイドカーの通信は **stdio 上の JSON-RPC（1行1メッセージ）** とする。進捗はサイドカーからの通知イベントとして流す。
- 進捗・完了・エラーのメッセージ型は、v2 の `analysisWorkerProtocol.ts` の `progress` / `done` / `error` の形をそのまま踏襲する。
- 音声データはメッセージに載せず、一時ファイルのパスをやり取りする。v2 のように長尺音源の `Float32Array` をレンダラで丸ごと保持する必要がなくなり、メモリ使用量が下がる。
- サイドカーが異常終了した場合、メインプロセスが検知してユーザーにエラーを提示し、一時ファイルを掃除すること。

### 8.3 モデル重みの管理

- 使用する重みは**全てアプリに同梱**する。初回起動時のダウンロードは行わない。
- モデル配布ホスト（Hugging Face の個人アカウント等）は実際に消滅する。参照先を上流に依存させず、**自身の GitHub Releases にミラーした版を正とする**。
- 各重みの sha256 をリポジトリに記録し、ビルド時に検証する。改竄・破損した重みが黙って読み込まれることを防ぐ。
- 参考値：`MelBandRoformer.ckpt` = 913,106,900 バイト / sha256 `87201f4d31afb5bc79993230fc49446918425574db48c01c405e44f365c7559e`、`rmvpe.pt` = 181 MB。

### 8.4 ビルド

- ターゲット：`--arm64` のみ。universal ビルドは行わない。
- **Windows ビルド（`build:win`）は廃止する。** PyTorch とモデルを同梱する構成では現実的でないため。
- 出力：DMG。
- モデル重みと Python 実行環境は asar に含めず、`extraResources` / `asarUnpack` 側に配置する。
- 署名：Developer ID を用いず、ad-hoc 署名を明示的に実行する。**Electron 本体に加え、同梱する全ての実行バイナリ（Python・ffmpeg・yt-dlp）に署名が必要**であり、ビルド後に署名の有無を検証する手順をビルドスクリプトに含める。
- `.dokokara` を `CFBundleDocumentTypes` に登録し、Finder からの関連付けを有効にする。

---

## 9. バックログ（将来の検討事項）

初期スコープには含めないが、設計上の余地を残しておく項目。

- 歌唱を録音し、後から聴き返せる機能
- 採点の加点要素（ビブラート・しゃくり・こぶし等）の判定
- 採点履歴の保存と推移グラフ
- LRC / 拡張 LRC（文字単位タイミング付き）へのエクスポート・インポート
- 移調（キー変更）・テンポ変更
- 複数プロジェクトのタグ付け・検索
- ボーカル分離の Core ML 化による高速化とアプリサイズ削減
- 分離モデルの差し替え（dereverb モデルの前段適用など）
- ライトテーマ・多言語 UI
- Intel Mac / Windows 対応
